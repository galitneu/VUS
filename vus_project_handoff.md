# פרויקט סרטוני הסבר VUS מותאמים אישית — מסמך מסכם להעברה ל-Claude Code

**גרסה:** 1.0
**תאריך:** מאי 2026
**מטרה:** מסמך מסכם של הדיון על תכנון הפרויקט, כך שניתן יהיה להמשיך אותו ב-Claude Code עם כל ההקשר.

---

## 1. תקציר ההפרויקט

פיתוח סדרת סרטוני וידאו חינוכיים על VUS (Variant of Uncertain Significance), מותאמים אישית למטופלות שקיבלו תוצאה כזו. הסרטונים הם **חלק ממחקר רחב יותר** של בוט תמיכה למטופלי VUS שחוקר boundary drift בכלי AI תחת לחץ מטופלים.

המטופלת יוצאת מייעוץ גנטי עם סיכום בידיה. היא מכניסה את הוריאנט/הסיכום למערכת, ומקבלת סרטון הסבר *מותאם להקשר ולוריאנט שלה*.

---

## 2. מטרת הסרטון

**מה הסרטון אמור לעשות:**
- להסביר מה זה VUS באופן כללי
- לחבר את ההסבר לוריאנט הספציפי של המטופלת (שם הגן, ההקשר הקליני)
- לתת תשתית קונספטואלית שמשלימה את הייעוץ הגנטי

**מה הסרטון לא אמור לעשות:**
- להמליץ על פעולות
- לתת ייעוץ קליני
- להחליף את היועצת הגנטית
- לדחוף להחלטות

---

## 3. החלטות מרכזיות שהתקבלו

### 3.1 רמת ההתאמה האישית (Level 2)

**רמה 1** (וריאנטים מוכנים מראש) — לא נבחרה.
**רמה 2** (תבנית פרמטרית עם החלפת תוכן) — **נבחרה לשלב הראשון**.
**רמה 3** (תוכן שנוצר מחדש על ידי AI לכל מטופלת) — לעתיד, אחרי שרמה 2 תוכח.

הסיבה: רמה 2 מאפשרת התאמה אמיתית בלי להכניס סיכוני hallucination שעולים ברמה 3.

### 3.2 ההקשר במחקר

המחקר מבחין בין שלושה הקשרים:
- **נשאות / VUS אישי** — לחץ נמוך
- **תכנון ילודה** — לחץ בינוני
- **הריון פעיל** — לחץ גבוה (edge case)

**ה-MVP מתמקד ב"הריון פעיל"** — מקרה הקצה. אם הסרטון עובד שם, הוא יעבוד בהקשרים הקלים יותר.

### 3.3 מקור המידע

- **ראשי:** סיכום שהמטופלת מכניסה לאחר ייעוץ גנטי. היועצת היא "המתווך האמין" שעשה כבר את הפרשנות הקלינית.
- **משני (אופציונלי):** ClinVar — להוספת הקשר על הגן והיסטוריית סיווגים. *לא* כמקור פרשני עצמאי.
- **לא במאגרים אחרים בשלב הראשון.**

### 3.4 טון וסגנון

**מתבסס על:**
- חינוכי-חם, *אבל פורמלי* (חינוכי-מקצועי, בסגנון סרטוני health literacy של איגודי רופאים)
- **גוף שלישי בלבד.** אין שימוש ב"את" (כי גם מנכר וגם פותר את בעיית המגדר בעברית).
- **ללא איורי דמויות אדם.** דיאגרמות, סמלים מדעיים, מופשטים.
- ללא ניסוח "סוכרי" או ילדותי.
- מכיר ברגש (אם בכלל) רק בעדינות, בלי לחבק.

**ויזואל מצופה:**
- פלטה כהה ומאופקת — כחול-נייבי + שמנת חם + זהב רך
- טיפוגרפיה: Frank Ruhl Libre לטקסט ראשי (סריף עברי קלאסי), Heebo רזה לטקסט משני
- תנועה מינימלית, ללא דרמטיות
- מוזיקה: מינימלית מאוד או בלעדיה
- קצב קריינות: ~150 מילים לדקה (איטי-מתון)

### 3.5 מבנה התסריט (שש פסקאות)

הסרטון בנוי ב-6 פסקאות + פתיחה וסיום. אורך כולל ~2:30.

1. **פתיחה** (15 שניות) — מסגור
2. **מהו וריאנט גנטי?** (25 שניות) — universal
3. **שלוש קטגוריות הסיווג** (25 שניות) — universal
4. **מה VUS לא אומר** (20 שניות) — universal
5. **הוריאנט הספציפי** (40 שניות) — **פרמטרי**
6. **למה זה עדיין לא ודאי** (25 שניות) — universal עם הקשר
7. **הסיווג משתנה עם הזמן** (25 שניות) — universal
8. **סיום** (15 שניות) — soft handoff ליועצת

רוב הסרטון משותף בין מטופלות. הפרמטריזציה מצטמצמת לפסקה 5 ולחלקים קטנים בפסקאות אחרות.

### 3.6 טכנולוגיה

- **Remotion** — framework לפלט הסופי (פרמטרי, דטרמיניסטי, מבוסס React)
- **HTML/CSS/JS** — לסקיצות ופרוטוטיפים בלבד
- **לא Higgsfield** — לא מתאים לפרויקט (גנרטיבי, פוטוריאליסטי, לא דטרמיניסטי, לא מתאים לאסתטיקה הפורמלית-חינוכית)

הסוכן (Claude Code) יבנה את הקוד ב-Remotion. המשתמשת לא לומדת React בעצמה.

---

## 4. הוריאנט הדוגמתי ל-MVP

**COL4A2 c.2552G>T (p.Gly851Val)**

- **ClinVar accession:** VCV2858375
- **סיווג:** VUS
- **סוג:** Missense
- **תורשה:** Autosomal dominant
- **De novo** — לא נמצא בהורים
- **לא תועד באוכלוסייה הכללית** (gnomAD = 0)
- **כלי ניבוי:** REVEL 0.936, SIFT 0.003, GERP 4.38 (deleterious)
- **ACMG criteria שיושמו:** PP3 (Moderate), PM2 (Moderate)
- **תפקוד הגן:** מקודד תת-יחידה של קולגן IV, רכיב מבני בקרומי הבסיס
- **מחלות קשורות בספרות:** מחלות של כלי דם קטנים במוח, porencephaly
- **הפנוטיפ בבדיקה:** פגמי לב מולדים בעובר (hypoplastic right heart, RV hypertrophy, tricuspid regurgitation, pulmonic stenosis)

**הערה חשובה:** יש *אי-התאמה פנוטיפית* — הגן בדרך כלל קשור למוח, הפנוטיפ הוא לב. זה חלק ממה שמשאיר את הוריאנט כ-VUS. בתסריט הנוכחי האי-התאמה רומזת אבל לא מפורשת.

---

## 5. סכמת הפרמטרים (לפסקה 5 ולחלקים שכפופים אליה)

```yaml
variant:
  gene_name: "COL4A2"
  variant_notation: "c.2552G>T (p.Gly851Val)"
  variant_type: "missense"  # missense | nonsense | splice | indel
  is_de_novo: true
  in_population: false
  prediction_strength: "deleterious"  # deleterious | benign | uncertain
  clinvar_accession: "VCV2858375"
  acmg_criteria: ["PP3", "PM2"]

gene:
  function_description: "מקודד תת-יחידה של קולגן IV, חלבון מבני בקרומי הבסיס"
  associated_conditions_in_literature: "מחלות של כלי דם קטנים במוח"

clinical_context:
  setting: "active_pregnancy"  # carrier | family_planning | active_pregnancy
  phenotype_present: true
  phenotype_description: "פגמי לב מולדים"  # optional - to handle phenotype mismatch
```

---

## 6. שאלות פתוחות

נקודות שעדיין לא הוכרעו:

1. **אסטרטגיית voice**:
   - TTS פרמטרי (ElevenLabs API) — גמיש, פחות חם
   - דובר/ת אנושי/ת קבוע/ה שמקליט/ה את כל הגרסאות — חם, אבל מגביל
   - היברידי — מורכב, נשמע מפוצל
   *פתוח. סביר ל-TTS אם רוצים לשמור על מסלול לרמה 3.*

2. **התייחסות מפורשת לאי-ההתאמה הפנוטיפית** (גן קשור למוח, ממצא בלב):
   - להשאיר רומז (כפי שעכשיו)
   - להפוך מפורש כדי לעזור להבין למה זה VUS
   *פתוח.*

3. **התייחסות לעובדה שהבדיקה נעשתה לעובר:**
   - הסרטון הנוכחי מדבר על "וריאנט שזוהה בבדיקה" באופן כללי, ללא הקשר ספציפי
   - יתרון: רב-שימושי
   - חיסרון: ניתוק רגשי
   *פתוח.*

4. **איך הסיכום מהיועצת מוכנס למערכת:**
   - טקסט חופשי + parsing על ידי מודל
   - טופס מובנה
   - העלאת מסמך (OCR)
   - היברידי
   *פתוח. כל אחד עם trade-offs שונים.*

5. **תפקיד הסרטונים מול הבוט במחקר:**
   - תנאי השוואה (במקום בוט)
   - התערבות לפני הבוט
   - זרוע מחקר נפרדת
   *פתוח. ההחלטה תשפיע על אורך, CTA, ומבנה.*

6. **אישור אתי לתרחיש הספציפי:**
   - אם הסרטון מושך מידע אמיתי מ-ClinVar למטופלת ספציפית, צריך לוודא שזה מכוסה באישור האתי של המחקר.

---

## 7. קבצים שכבר נוצרו

- `vus_script_draft_v1.md` — טיוטה ראשונה של התסריט המלא, עם הערות לעיבוד וסכמת פרמטרים
- `vus_opening_preview.html` — סקיצה ויזואלית של 15 השניות הראשונות (פתיחה), HTML/CSS/JS

---

## 8. המסלול הבא — שלבים ב-Claude Code

### שלב 1: הקמת תשתית Remotion
- אתחול פרויקט Remotion חדש
- התקנת תלויות (כולל תמיכה ב-Hebrew/RTL)
- בחירת גופנים: Frank Ruhl Libre + Heebo דרך Google Fonts
- הקמת design system בסיסי (משתני CSS לצבעים מוגדרים)

### שלב 2: תרגום הסקיצה לקומפוננטה ראשונה
- לקרוא את `vus_opening_preview.html` ולתרגם לקומפוננטה Remotion של הפתיחה
- לוודא שהמראה והקצב משחזרים את הסקיצה (15 שניות)
- לבדוק רנדור ל-MP4

### שלב 3: בניית מבנה פרמטרי
- מימוש סכמת הפרמטרים (סעיף 5) כממשק TypeScript
- בניית קומפוננטות לכל פסקה, עם props שמתקבלים מהסכמה
- מימוש פסקה 5 (הוריאנט הספציפי) כקומפוננטה הפרמטרית הראשונה

### שלב 4: בניית 5 הפסקאות הנותרות
- פסקה אחר פסקה, בהתאם לתסריט
- בדיקת רנדור אחרי כל פסקה

### שלב 5: אינטגרציית קריינות
- *החלטה ראשונה לקבל:* TTS או אנושי
- אם TTS: אינטגרציה עם ElevenLabs API דרך Remotion
- סנכרון אודיו עם ויזואל

### שלב 6: תרגיל אימות פרמטריזציה
- להחליף את COL4A2 בגן אחר (למשל BRCA2 או MLH1) באותה סכמה
- לרנדר את הסרטון השני
- לבדוק אילו משפטים נשברים ולחזק את הסכמה

---

## 9. עקרונות מנחים לסוכן

עקרונות שכדאי לכבד בכל שלב:

1. **הוויזואל פורמלי, לא קליני-קר ולא ילדותי-חם.** הימנעו מ-defaults של AI כמו לבן בוהק, סגול-ורוד, ופונטים גנריים.

2. **גוף שלישי בלבד בקריינות ובטקסט.** "כאשר מתגלה וריאנט..." ולא "כאשר את מקבלת תוצאה...". בדקו בקפדנות.

3. **אין דמויות אדם.** דיאגרמות, מולקולות מופשטות, גרפים — כן. דמויות מאוירות או מצולמות — לא.

4. **שמרו על דטרמיניזם.** אם אותם פרמטרים מוכנסים פעמיים, הפלט חייב להיות זהה.

5. **RTL בעברית עם טקסט מעורב באנגלית.** בדיקת רנדור של שמות גנים (COL4A2, BRCA1) בתוך משפט עברי היא דרישה חוזרת בכל פסקה. הזניחה תיצור באגים ויזואליים שלא תמיד נראים בקוד.

6. **רוב הסרטון משותף.** לפני שמפרמטרים משהו, לוודא שהוא באמת משתנה בין מטופלות. פרמטריזציית-יתר היא טעות נפוצה.

7. **כל החלטה מחקרית-קלינית עוברת דרך החוקרת.** הסוכן אינו ממציא תוכן רפואי, לא משנה ניסוחים שעלולים להשפיע על משמעות קלינית, ולא מוסיף פרשנות שלא הוגדרה בסכמה.

---

## 10. הסתייגויות

- כל ההחלטות לעיל פתוחות לדיון. סטטוס "הוחלט" הוא של הדיון הנוכחי, לא של הצוות המחקרי המלא.
- ההחלטה לעבור לרמה 3 בעתיד תדרוש שיקול מחודש של כל סעיפי הבטיחות המחקרית.
- המסמך לא מחליף את הברייף המחקרי של המחקר המקורי — הוא משלים.
